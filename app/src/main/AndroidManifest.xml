<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.kuleuven.cookr">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cookr">
        <activity android:name=".activities.viewRecipe"></activity>
        <activity android:name=".activities.ShoppingList" />
        <activity android:name=".activities.Favourites" />
        <activity android:name=".activities.Recipes" />
        <activity android:name=".activities.LogIn" />
        <activity android:name=".activities.SignUp" />
        <activity android:name=".activities.MainActivity" android:theme="@style/SplashScreenWindow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>